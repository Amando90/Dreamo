<%= stylesheet_link_tag	 'masonry', media: 'all' %>

<div id="masonry-container" class="transitions-enabled centered clearfix masonry">
  <% @posts.each do |post|  %>
    <div class="box panel panel-default">
    	  <h5><strong><%= link_to post.title.upcase, post %></strong></h5>
	  <%= link_to (image_tag post.image.url), post %>

	  <div class="submit-box">
	  	<center><small>Submitted by, <%= post.user.email %></small></center>
	  </div>
	</div>	
  <% end %>
</div>

<%= javascript_include_tag 'posts' %>